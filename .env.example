# For GitHub Pages, Vite’s base should match the repo subpath. CI sets this in workflows.
# VITE_BASE_PATH=/software-subscription-tracker/

# This app is static HTML in index.html. Do not place secrets in any client-facing file;
# for server APIs later, use environment-specific secret stores in the cloud.
